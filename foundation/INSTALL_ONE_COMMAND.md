Linux/macOS:

```bash
git clone https://github.com/shellworlds/ENVR.git && cd ENVR && bash foundation/setup_demo.sh
```

Windows (PowerShell + WSL recommended):

```powershell
git clone https://github.com/shellworlds/ENVR.git; cd ENVR; wsl bash foundation/setup_demo.sh
```
